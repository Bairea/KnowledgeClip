# KnowledgeClip 领域词汇表

多站点大模型官网站点聊天聚合器：一次输入并发推送到多个站点，聚合回答并支持
keep/remove 过滤导出。该领域模型聚焦"站点对话驱动"与"内容摘取"两条主线。

## 语言

**引擎（Engine）**：
驱动浏览器完成站点对话（发送、等待、提取）的执行单元集合，按优先级降级
（rod → browser-act → playwright-go）。bsk 是实验中的候选补充。
_Avoid_: 驱动层、后端进程

**内容摘取（Content Extraction）**：
把页面中目标内容元素（聊天回答 / 文章正文）转成结构化 Markdown 并取回的行为，
与"抓取"的区别在于保真结构（标题、代码块、表格）且只针对内容本体。
_Avoid_: 抓取、扒网页、解析页面

**回答（Answer）**：
站点 LLM 针对一次提问生成的最终内容，是摘取的目标产物；思考/推理块与 UI 控件
不属于回答。
_Avoid_: 回复文本、消息正文

**会话（Session）**：
一次提问在单一站点的完整往返（发送 → 等待稳定 → 摘取回答），以及跨站聚合后在
本应用内的记录。区别于 bsk/browser-act 的自动化会话（那属实现细节）。
_Avoid_: 对话、一次请求

**待收回答（Pending Answer）**：
已发送提问但回答尚未稳定下来的状态；引擎以文本稳定轮询判断完成。
_Avoid_: 等待中、生成中状态

**bsk**：
browser-skill 的 CLI，驱动用户真实 Chromium（含登录态）完成浏览器自动化，
全部子命令可脚本化调用、无需 LLM 参与摘取。
_Avoid_: 浏览器命令、自动化客户端

**Agent Window**：
bsk 为自动化任务开辟的隔离窗口；用户个人窗口不受影响，借用用户标签页需显式
借用且即时归还。
_Avoid_: 自动化窗口、控制窗口